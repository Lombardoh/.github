name: Test One
on:
  workflow_call:
    inputs:
      who:
        required: true
        type: string
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "one:${{ inputs.who }}"
