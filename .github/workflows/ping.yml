name: ping asd
on:
  push:
  pull_request:
  workflow_dispatch:
permissions: {}
jobs:
  ok:
    runs-on: ubuntu-latest
    steps:
      - run: echo "repo=$GITHUB_REPOSITORY event=$GITHUB_EVENT_NAME sha=$GITHUB_SHA"
